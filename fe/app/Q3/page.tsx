import { Flex, Typography } from "antd";




export default function Q3() {

    return(
        <Flex vertical>
            <Typography>
                Kita bisa menggunakan 'SELECT * FORM User INNER JOIN Company ON User.id = Company.user_id;'
            </Typography>
        </Flex>
    )
}